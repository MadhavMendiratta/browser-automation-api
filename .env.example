# Database — use "postgres" as hostname (docker-compose service name)
DATABASE_URL=postgresql+psycopg2://admin:admin@postgres:5432/scraper

# Auth
SECRET_KEY=change-me-to-a-random-secret

# Optional
API_KEY=none
CACHE_EXPIRATION_SECONDS=3600
